extends layout

block medidor
    // el - permite definir variables
    -const estilo = "width:25%;height:20px"
    // el simbolo de ≠ permite insertar html parseado en lugar del string lineal contenido en la variable
    // != igual a ≠
    h1(style="color:blue;")≠titulo
    span=min+" "
    meter(value=nivel min=min max=max style=estilo)
    span=" "+max !=